Decode and inspect JSON Web Tokens. Paste a JWT to see the decoded header and payload with annotated claims. Edit iat, exp, and nbf timestamps using date pickers and quick offset buttons (+30m, +1h, +1d, +7d). Verify HS256 signatures with a shared secret using the Web Crypto API. All decoded tokens are automatically saved to a localStorage history log with optional labels for quick access.
