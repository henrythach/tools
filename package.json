{
  "name": "html-tools",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{html,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "*.{html,md}": "prettier --write"
  }
}
